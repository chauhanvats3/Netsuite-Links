https://tstdrv1150860.app.netsuite.com/app/center/card.nl //open two instances together

=======================================WORKING SOLN=================

var recType = nlapiGetRecordType();
if(recType !=void 0){
var recId = nlapiGetRecordId();
var rec = nlapiLoadRecord(recType,recId);
var myJson = JSON.stringify(rec);
var jsonObj = JSON.parse(myJson);
var jsonPretty = JSON.stringify(jsonObj, null, '\t');
var x = window.open();
    x.document.open();
    x.document.write('<html><body><pre>' + jsonPretty + '</pre></body></html>');
    x.document.close();
}else{
	alert("Not A record Page!");
}
